@using PadWiki.WebClient.Services.Contracts
@inject ICardService CardService

<Pagination Count="CardService.Cards.Count"/>

<ul class="d-flex flex-wrap justify-content-between" style="padding-left: initial;" id="cardResults">
    @foreach (var card in CardService.Cards)
    {
        <li class="poke-card m-2">
            <PokemonCard Image="@card.ImagePath" Title="@card.Title" Description="@card.Description" CardRarity="card.CardRarity" CardDropRate="card.DropRarity"/>
        </li>
    }
</ul>

<Pagination Count="CardService.Cards.Count"/>

@code {
}